name: external-dns
displayName: ExternalDNS
source: https://github.com/kubernetes-sigs/external-dns
lastVerified: "2026-06"
# No official version table; broadly compatible, tracks the client-go it ships with. All best-effort.
rules:
  - addonRange: ">=0.14.0 <0.15.0"
    k8sRange: ">=1.24.0 <=1.30.0"
    bestEffort: true
  - addonRange: ">=0.15.0"
    k8sRange: ">=1.26.0 <=1.35.0"
    bestEffort: true
