name: prometheus
displayName: kube-prometheus-stack / prometheus-operator
source: https://github.com/prometheus-operator/kube-prometheus#compatibility
lastVerified: "2026-06"
# kube-prometheus publishes a compatibility matrix per release; rows here are best-effort until re-verified.
rules:
  - addonRange: ">=0.68.0 <0.75.0"
    k8sRange: ">=1.24.0 <=1.30.0"
    bestEffort: true
  - addonRange: ">=0.75.0"
    k8sRange: ">=1.28.0 <=1.35.0"
    bestEffort: true
